<?xml version="1.0" encoding="utf-8"?>
<!-- Compatible con Amazon Fire TV + Android TV (Leanback). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Fire TV / Android TV: no requieren pantalla táctil -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <!-- No exigir OpenGL ES alto (sticks antiguos) -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <!-- Fire OS opcional: no bloquear Android TV genérico -->
    <uses-feature
        android:name="amazon.hardware.fire_tv"
        android:required="false" />

    <application
        android:banner="@drawable/tv_banner"
        android:largeHeap="true"
        android:hardwareAccelerated="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:resizeableActivity="false"
            android:supportsPictureInPicture="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
